라우팅
경로 지정을 파일시스템으로 한다. 이게 app기준이여서 app routing

app이라는 디렉토리안에 폴더를 정리하고 그 url path와 일치하는 형식 

정적 라우팅이 있고, 동적 라우팅이 있다.

파일 시스템을 기반으로 한다. 그래서 폴더 구조를 어떻게 짜냐에 따라서 여러가지 방식으로 라우팅으로 할수있고

그 경로에 이름에 맞게 규칙되어있는 친구를 세그먼트라고 부른다.

패이지간 이동
Link href="/dashboard" 이렇게 한다.

버튼을 클릴할때는 
useRouter() 호출해서 router.push 이렇게하면되는데 꼭 클라이언트 컴포넌트로 정의해야한다. 